import React from "react";

// LineHaul Station wordmark for the Payload admin (login screen + nav header).
// Uses the light horizontal logo (for the carbon dark admin theme).
export const Logo = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img
    src="/assets/logo-horz-light.png"
    alt="LineHaul Station"
    style={{ width: 240, maxWidth: "100%", height: "auto" }}
  />
);

export default Logo;
